These are some of the ML models to identify which features were more relevant in predicting CO performance (PSA; 2020). The best model was the Logistic Regression (a=.89). Through this model we were able to support the selection of candidates to follow the training.
The pre-processing (cleaning, data exploration, feature selection and feature engineering) had already been done in another script.
